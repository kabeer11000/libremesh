# LibreMesh Access Control - Allow all for PHP files

# Apache 2.4 - use mod_authz_core
<IfModule mod_authz_core.c>
    # Deny nothing - allow all PHP files
    Require all granted
</IfModule>

# Apache 2.2 - fallback
<IfModule !mod_authz_core.c>
    Allow from all
</IfModule>